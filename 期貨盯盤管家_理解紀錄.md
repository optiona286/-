# 期貨盯盤管家理解紀錄

## 本次讀取範圍

- 已讀取截圖：`15482.jpg`、`15553_0.jpg`、`15554_0.jpg`、`15555_0.jpg`、`15557_0.jpg`、`15558_0.jpg`、`15559_0.jpg`、`15560_0.jpg`
- 已檢查資料夾：`2026`
- `2026` 內目前看到 45 個 `.zip` 交易日資料檔
- 抽樣讀取檔案：`2026/Daily_2026_03_03.zip`
- 該 zip 內含一個 `Daily_2026_03_03.rpt`

## 抽樣資料格式理解

`Daily_2026_03_03.rpt` 是逗號分隔文字資料，表頭如下：

```text
成交日期,商品代號,到期月份(週別),成交時間,成交價格,成交數量(B+S),近月價格,遠月價格,開盤集合競價
```

我目前理解資料載入規則如下：

1. 從使用者上傳或指定的資料夾中掃描每日 zip。
2. 每個 zip 內讀取 `.rpt` 檔。
3. 只保留 `商品代號` 去除空白後等於 `TX` 的資料。
4. 以 `到期月份(週別)` 分組。
5. 對每個月份契約加總 `成交數量(B+S)`。
6. 選出成交量最大的那個月份契約，作為當日基準資料。
7. 後續行情圖、監控波動與回測都以這個契約的成交序列為主。

抽樣檔 `Daily_2026_03_03.zip` 的 TX 成交量分組結果中，最大者為：

```text
TX 202603
成交量 410062
```

該契約抽樣統計：

```text
筆數 155263
開 35099
高 35366
低 34314
收 34325
量 410062
```

注意：資料中也有 `202603/202604` 這類跨月份價差項目。由於你要的是「成交量最大的那一個月份的契約」，我目前會把基準資料理解為單一月份契約，例如 `202603`，不使用價差契約作為主圖與回測基準。

## 截圖中的主要產品邏輯

這個產品的核心不是一般看盤報價，而是「用台股期貨的早盤 / 晚盤波動，產生可監控、可通知、可回顧的事件」。

主流程可理解成：

1. 使用者選擇早盤或晚盤。
2. 系統顯示該盤別的台股期貨目前成交價、漲跌點數、漲跌幅、成交量。
3. 以參考價為基準，計算目前價格上漲或下跌幾點。
4. 圖表顯示價格走勢、參考價線與成交量。
5. 下方事件清單顯示觸發過的波動門檻，例如「下跌 350 點 到 37698」。
6. 使用者可進入設定頁調整通知條件，例如早盤 / 晚盤是否啟用、每幾點推播一次、進階監控項目是否開啟。

## 畫面與互動理解

### 監控波動首頁

可見元素：

- 上方藍色導覽列。
- 左側齒輪圖示，推測進入監控設定。
- 中間早盤 / 晚盤分段切換。
- 右側訊息或通知圖示，帶橘色未讀提示點。
- 商品名稱：例如 `台股期貨早盤`、`台股期貨晚盤`。
- 報價摘要：成交價、漲跌點數、漲跌幅、成交量。
- 行情資訊列：開、高、低、參考價、查價線開關。
- 價格圖：紅色表示高於參考價，綠色表示低於參考價。
- 下方成交量柱。
- 事件清單標題：`參考價 xxxxx 監控中`。
- 事件列表：上升 / 下跌幾點、到達價格、觸發時間。

互動邏輯：

- 點早盤 / 晚盤會切換盤別資料、圖表與事件清單。
- 查價線開關應該是圖表查價輔助線。
- 齒輪可進入監控通知設定。
- 事件列表應該按觸發時間或門檻接近程度排序，截圖看起來偏向最近觸發在上。

### 國際行情

可見元素：

- 頁面標題 `國際行情`。
- 頂部仍保留台股期貨晚盤摘要，代表國際行情是輔助參考，不是主商品切換。
- 分頁：`國際股市`、`美股科技`、`美股非科技`、`台股ADR`。
- 國際股市列表有國旗、名稱、價格、漲跌與漲跌幅。
- 台股 ADR 列表有中文名稱、英文代號、價格、漲跌與漲跌幅。
- ADR 頁底顯示資料日期，例如 `以上為04/24(五)收盤價`。

互動邏輯：

- 點分頁切換不同國際市場清單。
- 每個清單是輔助監控資料，與主監控的台股期貨摘要同屏呈現。

### 籌碼日報

可見元素：

- 頁面標題 `籌碼日報`。
- 日期，例如 `04/24(五)`。
- 加權指數收盤點位、漲跌、漲跌幅、成交金額。
- 右側有大字情緒判斷，例如 `強多`。
- 下方是條列籌碼數據：
  - 外資淨未平倉
  - 外資淨未平倉加減
  - 散戶淨未平倉
  - 散戶淨未平倉加減
  - 三大法人買賣超
  - 外資買賣超
  - 投信買賣超
  - 自營商買賣超
  - 官股買賣超
  - 融資變動
  - 融券變動

互動邏輯：

- 這頁偏日報，不像即時監控。
- 重點是把多空籌碼資料轉成簡單判斷，例如強多 / 強空 / 中性。

### 更多功能

可見元素：

- 使用者資訊：暱稱、帳號、權限。
- 登出按鈕。
- 功能列表：升級專業版、分享好友、APP 評分、給我們建議、聯繫客服、螢幕恆亮。
- 推薦工具：價值 K 線、籌碼 K。
- 底部公司資訊、服務條款、隱私權政策、著作權政策、版本號。

互動邏輯：

- 多數列項右側有箭頭，代表進入下一頁或外部連結。
- `螢幕恆亮` 看起來是可切換設定。

### 監控波動設定頁

可見元素：

- 上方返回箭頭。
- 標題 `監控波動`。
- 右上 `調整`。
- 監控通知區：
  - 早盤 ON/OFF
  - 晚盤 ON/OFF
  - `10-100點`：漲跌每 10 點推播
  - `120-200點`：漲跌每 20 點推播
  - `超過200點`：漲跌每 50 點推播
- 進階監控區：
  - 欄位分早盤、晚盤。
  - 觸及外資成本價
  - 突破今日最高點
  - 跌破今日最低點
  - 突破前盤最高點
  - 跌破前盤最低點
  - 觸及開盤價
- 訂盤管家區：
  - 早盤 ON/OFF
  - 晚盤 ON/OFF
  - 漲跌超過 100 點，每 20 點收到一次推播通知

互動邏輯：

- ON/OFF 控制是否啟用該盤別通知。
- 進階監控用 checkbox 控制不同事件是否要在早盤 / 晚盤觸發。
- `調整` 應該可以進一步修改門檻或通知設定。

## 我打算怎麼用這些理解

我不會一比一復刻截圖，而是取它的核心邏輯：

1. 以 `TX` 作為唯一商品來源。
2. 每個交易日自動選成交量最大的單一月份契約。
3. 用該盤第一筆秒級成交價作為參考價，建立漲跌點數監控。
4. 將行情圖、事件清單、通知設定、回測結果整合成同一套邏輯。
5. 早盤 / 晚盤要作為重要維度，因為截圖裡幾乎所有監控設定都分早晚盤。

## 資料夾上傳功能理解

你提到「要有可以上傳的資料夾功能」，我理解為：

1. 使用者選擇一個本機資料夾，例如 `2026`。
2. 系統掃描資料夾中的每日 zip。
3. 系統不要求使用者逐一選檔。
4. 系統解析每個 zip 內的 `.rpt`。
5. 每日只載入 `TX` 且成交量最大的月份契約。
6. 載入後可供看盤、事件監控、回測使用。

這裡我會避免要求使用者手動指定契約月份，因為你已明確說要用成交量最大的月份契約作為基準。

## 已確認規格

### 回測目的

回測只用來驗證與回放「監控事件是否觸發」，不做交易策略損益。

也就是說，這一版回測不處理：

- 進場
- 出場
- 停損
- 停利
- 部位口數
- 手續費
- 滑價
- 損益曲線

後續如果要做交易策略損益，再另外設計。

### 參考價

參考價使用該盤別第一筆成交資料的價格。

- 早盤：早盤時段第一筆成交價。
- 晚盤：晚盤時段第一筆成交價。
- 資料以秒為時間單位，因此參考價取該盤第一個成交秒的第一筆價格。

### 早盤與晚盤切分

早盤 / 晚盤切分規則：

```text
早盤 08:45-13:45
晚盤 15:00-05:00
```

每個每日檔案都以該檔案內的資料為準處理。因為晚盤跨日，所以例如 `Daily_2026_03_03.zip` 可能包含前一日 15:00 之後的晚盤資料，以及檔案日期當日的早盤資料。

### 契約選擇

契約選擇規則：

1. 先篩選 `商品代號 = TX`。
2. 依 `到期月份(週別)` 分組。
3. 加總 `成交數量(B+S)`。
4. 選成交量最大的契約。
5. 沒有次順位或備援契約邏輯。

### 三關價

`觸及三關價` 沒有要做，從目前功能規劃中刪除。

## 回測功能目前理解

你提到第二個缺少部分是「回測功能」。依照截圖目前的產品邏輯，我先理解成：

1. 使用歷史 TX tick 資料重播某一天或一段日期。
2. 依早盤 / 晚盤切分資料。
3. 以該盤第一筆成交價作為參考價。
4. 依監控規則產生事件，例如：
   - 每 10 / 20 / 50 點觸發一次漲跌通知。
   - 突破今日最高點。
   - 跌破今日最低點。
   - 突破前盤最高點。
   - 跌破前盤最低點。
   - 觸及開盤價。
5. 回測結果顯示觸發時間、觸發價格、觸發類型、當時漲跌點數。

## 我認為還需要加強的地方

### 1. 資料載入狀態要清楚

資料夾上傳後應該清楚顯示：

- 掃到幾個 zip。
- 成功解析幾個交易日。
- 每日選到哪個 TX 契約。
- 是否有檔案缺資料、格式錯誤、沒有 TX、沒有早盤或沒有晚盤。

這很重要，因為後面所有圖表與回測都依賴資料是否正確載入。

### 2. 盤別資料要可檢查

每一天最好能顯示早盤 / 晚盤基本統計：

- 參考價
- 開盤第一筆時間
- 最後一筆時間
- 最高
- 最低
- 收盤或最後成交價
- 成交量
- 觸發事件數

這樣可以快速確認跨日夜盤有沒有切錯。

### 3. 回測結果要能追溯

回測不只列事件，還應該能點選或定位到圖表上的觸發時間。

事件列表建議至少包含：

- 盤別
- 觸發時間
- 觸發價格
- 相對參考價漲跌點數
- 事件類型
- 所屬交易日
- 所屬契約

### 4. 設定與回測要共用同一套規則

監控頁與回測頁不能各算各的。通知門檻、早晚盤開關、進階監控項目，應該共用同一份設定，這樣回測結果才會等於實際監控邏輯。

## 手機版 HTML 交互與功能規劃

### 整體形式

目前先用單一 `index.html` 製作，不先架後端。

設計方向以手機操作為主：

- 第一屏就是工具本身，不做形象首頁。
- 上方保留早盤 / 晚盤切換。
- 底部固定分頁，方便單手操作。
- 主要資訊用大字呈現，清單列高要適合手指點選。
- 資料載入、監控、回測、設定拆成清楚的分頁。

### 底部分頁

目前規劃四個分頁：

1. `監控`
   - 顯示台股期貨早盤 / 晚盤。
   - 顯示成交價、漲跌點數、漲跌幅、成交量。
   - 顯示開、高、低、參考價、契約月份。
   - 顯示價格走勢圖與成交量。
   - 顯示依參考價觸發的事件清單。

2. `回測`
   - 不再作為獨立報表頁。
   - 作為回測控制台使用。
   - 可選擇交易日與盤別後進入監控頁播放。
   - 真正的回測行情、事件觸發、暫停、前進、快進都在監控頁進行。

3. `資料`
   - 上傳整個資料夾。
   - 只先掃描資料夾內每日 zip，建立日期清單。
   - 不會在選資料夾後立刻解析所有 zip。
   - 使用者點選某一日後，才解壓縮該日 zip 內 `.rpt`。
   - 單日解析時只讀取 `TX`。
   - 排除價差契約，只挑單一月份契約中成交量最大者。
   - 點選完成後切回監控頁，用該日資料進行回測播放。

4. `設定`
   - 早盤 / 晚盤 ON/OFF。
   - 固定波動門檻：
     - 10-100 點，每 10 點觸發。
     - 120-200 點，每 20 點觸發。
     - 超過 200 點，每 50 點觸發。
   - 進階監控保留：
     - 觸及外資成本價
     - 突破今日最高點
     - 跌破今日最低點
     - 突破前盤最高點
     - 跌破前盤最低點
     - 觸及開盤價
   - 已刪除三關價。

### 國際行情與籌碼日報

你目前的期貨資料沒有國際行情與籌碼日報資料。

這兩個功能先不阻塞主功能，後續可走 API 串接：

- 若 API 可用，再補國際行情與籌碼日報分頁或區塊。
- 若 API 暫時不可用，先保留核心期貨監控與回測。

### 已開始動工

已建立第一版 `index.html`，目前包含：

- 手機版四分頁 UI。
- 資料夾上傳後建立日期清單。
- 點選某一日才前端 zip 讀取與 `.rpt` 解壓縮。
- `TX` 單一月份契約成交量最大者選取。
- 早盤 / 晚盤切分。
- 該盤第一筆成交價作為參考價。
- 監控圖表。
- 線圖以每分鐘為單位聚合，該分鐘最後成交價作為線圖點位，成交量以分鐘加總。
- 線圖參考截圖做參考價上下漸層：參考價上方偏紅、下方偏綠。
- 事件清單。
- 監控頁內回測播放。
- 暫停、播放、前進、快進、秒數控制。
- 回測控制列平常隱藏，只有進入回測播放狀態時才可用。
- 回測中可用小按鈕隱藏控制列；隱藏後右下角顯示小型 `回測控制` 按鈕，可再打開。
- 手機即時通知開關。
- 內建上漲 / 下降通知音效：上漲為上行雙音，下降為下行雙音，不依賴外部音效檔。
- 設定頁提供通知音效開關與音效測試按鈕。
- 漲跌超過 100 點時使用重大波動音效，並用瀏覽器語音播報「盯盤管家提醒，目前上漲/下跌超過100點」。
- 設定頁提供百點語音提醒開關與測試按鈕。
- 設定頁。
- 版面改為全版響應式網站，不再刻意限制成固定手機外框寬度。
- 已開始改成 PWA：
  - 新增 `manifest.webmanifest`
  - 新增 `sw.js`
  - 新增 `offline.html`
  - 新增 `assets/icon.svg`
  - `index.html` 已註冊 service worker
  - 新增本機測試用 `server.js`
  - PWA 需透過 `localhost` 或 HTTPS 開啟，`file://` 無法完整啟用 service worker

## 目前唯一需要確認的問題

價差契約例如 `202603/202604` 是否永遠排除？

我的建議是排除，因為它不是單一 TX 月份契約，價格也不適合拿來當台指期主圖與漲跌點監控基準。若你要的是「只看單一月份契約中成交量最大者」，那目前文件已照這個方向整理。


目前還有什麼問題嗎 ？

你先告訴 你要怎麼 做交互 功能 有哪些 

我可以先告訴你  用 html做出來程式 目前還是要以你手機操作上為主 希望你用手機的uxui來做 
至於  國際行情 籌碼日報  我的期貨資料是沒有的 所以 我個人會想要以 api串接 來去串接資訊 沒辦法也沒有差 

如果沒有問題 就開始動工吧 
